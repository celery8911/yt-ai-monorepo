# ============================================
# NPM 私仓配置
# ============================================

# 私仓地址：将包发布和下载指向本地 Verdaccio 服务
# 注意：CI 环境无法访问 localhost，所以这里注释掉
# 本地发布时使用命令：pnpm publish --registry http://localhost:4873/
# 或者取消下面这行的注释（仅限本地使用）
# registry=http://localhost:4873/

# ============================================
# PNPM 配置
# ============================================

# shamefully-hoist（提升依赖）
# 作用：将依赖提升到 node_modules 根目录，而不是嵌套在 .pnpm 目录中
#
# 默认情况（false）：
#   node_modules/
#     .pnpm/                    # 所有依赖都在这里（严格隔离）
#     package-a -> .pnpm/...
#
# 启用后（true）：
#   node_modules/
#     package-a/                # 依赖直接提升到根目录（兼容性更好）
#     package-b/
#
# 优点：
#   - 兼容一些不规范的包（直接 require 未声明的依赖）
#   - 某些工具期望依赖在 node_modules 根目录
#
# 缺点：
#   - 失去了 pnpm 的严格依赖隔离特性
#   - 可能隐藏依赖声明问题
#
# 建议：monorepo 项目通常需要开启以避免兼容性问题
shamefully-hoist=true